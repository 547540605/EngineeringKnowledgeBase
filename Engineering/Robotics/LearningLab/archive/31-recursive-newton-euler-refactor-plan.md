# 笔记 31 非破坏性拆分规格：递归 Newton–Euler 动力学

## 发现的问题

当前文件 [`31-recursive-newton-euler-dynamics.html`](31-recursive-newton-euler-dynamics.html) 有 **3963 行**，超出 README 规定的“单篇 HTML ≤1500 行”。问题不是正文单纯过长，而是一个页面同时塞进了：

1. Newton / Euler 基础方程与 RNEA 双向递推；
2. 连杆隔离体图解；
3. 两连杆交互仿真台；
4. 大量 canvas 绘制与 modal 交互脚本；
5. 四个力矩项的零跳步深推导模板。

这使得任意一处布局或交互问题都很难定位，也违反当前的笔记体积约束。

## 拆分目标

保持“笔记 31 = 递归 Newton–Euler 完整算法”这个学习主题不变，但拆为一个短入口页加四个职责单一的子页。任何单页 HTML 都控制在 1500 行内；长 JavaScript 放到独立 `.js` 文件，不能继续埋在 HTML 中。

```text
31-rnea-overview.html                         # 31 总览入口：< 900 行（已落地）
31-recursive-newton-euler-dynamics.html      # 历史超大页：仅保留，等待内容逐项迁移审计
31a-newton-euler-force-balance.html          # 牛顿/欧拉方程 + 单杆隔离体：< 1200 行（已落地）
31b-rnea-forward-backward-pass.html          # 外推/内推与关节力矩提取：< 1200 行（已落地）
31c-rnea-two-link-simulation-lab.html        # 两连杆交互仿真：HTML < 900 行（已落地）
31c-rnea-two-link-simulation-lab.js          # canvas/slider/绘图逻辑，可独立维护（已落地）
31d-rnea-four-torque-terms.html              # 四类力矩的逐项深推导：< 1400 行（已落地）
```

> 允许子页使用 31a/31b/31c/31d 编号；它们不是新增主课程号，而是笔记 31 的可独立阅读模块。

## 内容归属（必须逐项保留）

| 原文件区段 | 新位置 | 保留目的 |
| --- | --- | --- |
| 标题、工程导读、RNEA 全局数据流、学习路线 | `31` 总览 | 让读者先知道“先外推运动，再内推力/矩”的全局因果 |
| §6.4 牛顿方程、欧拉方程、质心惯性力/惯性矩 | `31a` | 将刚体单杆物理本体与递推算法分开 |
| 连杆 i 隔离体图、相邻连杆作用力/反作用力 | `31a` | 解释内推的受力来源，防止只背公式 |
| §6.5 RNEA 外推量、内推量、末端负载边界 | `31b` | 给出完整算法顺序与每个变量的 frame/物理含义 |
| $τ_i = n_i^T \hat z_i$ 的关节轴投影 | `31b` | 说明空间力矩怎样变成电机关节标量 |
| 两连杆参数滑块、数值向量、canvas 图、实时结果 | `31c` + `.js` | 让交互只服务“参数改变 → 加速度/力/力矩如何改变” |
| 欧拉自身转动、下游反作用、质心惯性杠杆、末端负载四项的 modal 内容 | `31d` | 不再用超长弹窗隐藏核心推导；改为可直读、可链接的分段解释 |
| 工程总结、与 CTC/重力补偿/ROS2-18 的桥接 | `31` 总览尾部 | 将本讲放回岗位能力主线 |

## 重构原则

### 1. 先复制验证，再删除旧段

1. 为每个目标页面建立完整但精简的 HTML 壳、导航和统一视觉变量；
2. 从原页面迁入一个模块；
3. 人工逐段核对公式、符号、图、引用和解释没有丢失；
4. 在浏览器检查该子页无横向溢出、Canvas/按钮可用；
5. 仅当新页已可访问，才从原页移除对应内容；
6. 最后将原 31 收缩为总览和子页导航。

禁止用“删除 2500 行然后重写摘要”的方式满足行数限制；那会把原本已经完成的深推导和数值实验变成不可追溯的内容损失。

### 2. HTML 与 JavaScript 分离

原 31 的主脚本从约第 917 行持续到约第 3341 行，是页面行数爆炸的主要来源。迁移 `31c` 时：

```html
<!-- 31c 页面仅保留画布、数值面板、slider 和可访问的文字说明 -->
<script defer src="31c-rnea-two-link-simulation-lab.js"></script>
```

JavaScript 必须：

- 在 `DOMContentLoaded` 后查找元素；
- 所有 DOM id 都局限在 `31c`；
- 计算函数与绘图函数分开；
- 无 Canvas 时仍显示关键公式和数值解释；
- 不依赖原 31 页面遗留的全局变量或 modal template；
- 将参数单位、默认值和数值边界集中定义。

### 3. 交互不能替代解释

`31c` 的每个 slider 旁边必须写明：改变的物理量、单位、作用在哪一条公式、预期改变哪一个结果。不要再把“看动画”当成理解证明。

### 4. 取消四大推导的深层 Modal 依赖

原页面将大量核心推导塞在 modal `<template>` 中，导致正文短、隐藏内容巨大，阅读上也不利于检索。`31d` 改为四个顺序可读的 section，并在 `31b` 中用普通链接跳转：

```text
① 自身转动欧拉惯性矩
② 下游连杆/末端的反作用力矩
③ 质心惯性力的力臂矩
④ 末端外载荷的力臂矩
```

只保留“查看大图”的轻量 modal；公式推导本身不再藏在弹窗里。

## 页面间导航要求

每页页首和页尾都给出：

```text
动力学总目录 → 29（空间加速度）→ 30（小臂质心）→ 31（RNEA 总览）
31a（单杆受力）→ 31b（递归算法）→ 31c（数值仿真）→ 31d（四类力矩推导）
下一主线：轨迹规划 / PID / 计算力矩控制
```

当前 `00-progress.html` 已修正为分别链接 29、30、31；重构完成后，31 总览仍保留原 URL，以免旧链接失效。

## 验收清单

### 内容完整性

- [ ] 原 31 中的所有主公式在至少一个新页面可找到；
- [ ] 四类力矩的“物理来源、方向/力臂、公式、单位”均完整保留；
- [ ] `n_i`、`f_i`、`P_i`、`P_{Ci}`、`\hat z_i` 均首次出现即定义坐标系和方向；
- [ ] 外推和内推不会混在同一张没有步骤的图里；
- [ ] 总览明确连接到计算力矩控制：RNEA 提供的是给定 $q,\dot q,\ddot q$ 时的逆动力学力矩。

### 交互与布局

- [ ] 任一 HTML 文件行数 ≤1500；
- [ ] 宽屏和约 768px 窄屏下公式、标签、Canvas 都不叠字/溢出；
- [ ] 每个按钮、slider、modal 都有真实可观测行为；
- [ ] 无 JavaScript 时仍可读到主要公式和推导结论；
- [ ] 不再有“右图看不清、向量方向变了、文字叠在一起”的信息丢失。

### 数值/物理一致性

- [ ] slider 的单位与结果单位一致（角速度 rad/s、角加速度 rad/s²、质量 kg、长度 m、惯量 kg·m²、力 N、力矩 N·m）；
- [ ] 默认参数下，数值结果能通过量纲检查；
- [ ] 改变小臂质量只影响与小臂相关的内推项，不会神秘改变底座平动项；
- [ ] 末端负载设为零时，外载荷项归零；
- [ ] 角加速度设为零时，欧拉角加速项归零，但向心/速度耦合项不应被误删。

## 与后续课程的交接

拆分后的 31 不需要重复讲完整 PID 或阻抗；它只输出一件事：

```text
给定 q, q̇, q̈、质量/惯量、重力和外载荷
→ 递归 Newton–Euler 算出每个关节需要的 τ
```

后续课程分别消费这个输出：

```text
轨迹规划：提供 q_d(t), q̇_d(t), q̈_d(t)
PID：根据 q_d - q 生成反馈项
CTC：RNEA 的 τ_ff + PD 反馈项
阻抗/导纳：在接触条件下修正参考或期望力
ROS2-18：将模型、轨迹约束、补偿与柔顺放进工程边界
```

这就是为什么 31 必须先拆清楚，而不应继续往同一页面塞 PID、CTC 或力控内容。
