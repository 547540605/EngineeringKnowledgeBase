# 笔记结构整改方案：04 移入专题支撑，主线编号重新接续

## 一、最终目标

当前的 `04-linear-algebra-homogeneous-coordinates.html` 不属于机器人主线的下一章，而是对齐次坐标、线性变换和仿射变换的线代补充。

因此采用以下结构：

- 04 从主线中移出；
- 04 放入“专题专栏·基础支撑”；
- 专题支撑区不使用主线编号；
- 原主线 05 重新编号为 04；
- 原主线 06 重新编号为 05；
- 后续所有主线章节统一向前挪一位；
- 主线最终保持 `01 → 02 → 03 → 04 → 05 ...` 连续编号。

这不是把 04 改成“可选的编号章节”，而是把它彻底从主线编号体系中拿出来。

## 二、调整后的目录逻辑

### 主线

```text
第一部分 · 坐标与变换
  01 坐标系中的点
  02 齐次变换矩阵
  03 坐标变换链

第二部分 · 旋转与两连杆
  04 二维两连杆正运动学       ← 原 05
  05 三维单轴旋转             ← 原 06
  06 三维机械臂坐标系           ← 原 07

阶段自测 · 基础
  07 基础复习自测             ← 原 08

第三部分 · DH 与正运动学
  08 DH 坐标系分配             ← 原 09
  09 单节 DH 变换              ← 原 10
  10 DH 表到正运动学           ← 原 11

阶段自测 · DH
  11 DH 复习自测               ← 原 12

第四部分 · 逆运动学
  12 二维两连杆逆运动学         ← 原 13

阶段自测 · 正逆运动学
  13 正逆运动学混合自测         ← 原 14

第五部分 · 雅可比与微分运动
  14 二维两连杆雅可比矩阵       ← 原 15
  15 二维两连杆奇异位形         ← 原 16
  16 二维两连杆的速度雅可比     ← 原 17
  17 二维两连杆的逆速度运动学   ← 原 18
  18 用逆速度让末端靠近目标     ← 原 19
  19 位置随时间变化与速度       ← 原 20

阶段自测 · 雅可比与速度
  20 雅可比与速度运动学自测     ← 原 21

第六部分 · 空间刚体速度
  21 空间刚体上两点的速度       ← 原 22
  22 速度递推：一节怎样传给下一节 ← 原 23
  23 三维角速度递推             ← 原 24
  24 三维线速度递推             ← 原 25
  25 两连杆速度递推实算         ← 原 26

第七部分 · 机械臂动力学与控制
  26 单关节动力学               ← 原 27
  27 多连杆空间角速度与角加速度递推 ← 原 28
  28 单连杆线加速度空间解耦     ← 原 29
  29 两连杆空间加速度两级外推   ← 原 30
  30 小臂质心正交解耦与牛顿力标定 ← 原 31
  31 递归 Newton-Euler 动力学算法 ← 原 32
```

### 专题支撑区

```text
专题专栏 · 基础支撑
  线性代数与齐次坐标
  矩阵乘向量列视角与坐标变换本质
  刚体转动本质与四角记号法字典
  求导顺序之谜：坐标导数与投影速度
  动力学前置补课：力矩、惯量与物理直觉
  空间惯性张量与欧拉转动方程深入解析
```

专题区的条目不要显示“04”“附录 0”等会与主线编号混淆的编号。可以使用标题和标签区分，例如：

```text
线性代数与齐次坐标    [理论支撑]
矩阵乘向量列视角      [基础专栏]
刚体转动本质          [基础专栏]
```

## 三、文件重命名策略

为了让文件名、页面标题、目录编号保持一致，主线文件也要统一重命名：

- 原 `05-planar-two-link-forward-kinematics.html` → `04-planar-two-link-forward-kinematics.html`
- 原 `06-3d-single-axis-rotations.html` → `05-3d-single-axis-rotations.html`
- 原 `07-3d-arm-coordinate-frames.html` → `06-3d-arm-coordinate-frames.html`
- 原 `08-foundation-review-quiz.html` → `07-foundation-review-quiz.html`
- 原 `09-...` → `08-...`
- 依此类推，原 `32-...` → `31-...`

04 的文件不要再叫“主线 04”。建议改成不带主线序号的文件名，例如：

```text
linear-algebra-homogeneous-coordinates.html
```

页面标题改为：

```text
专题：线性代数与齐次坐标
```

如果担心旧链接失效，可以保留一个旧文件名的跳转页，但正式导航只能指向新的无编号专题文件。

## 四、必须同步更新的地方

重命名不能只改文件名，需要全仓库同步替换引用。

### 1. `robotics-notes/index.html`

- 第一部分只保留 01、02、03；
- 第二部分从新的 04 开始；
- 后续所有主线编号使用新编号；
- 删除主线中的旧 04；
- 在“专题专栏·基础支撑”中加入无编号的线性代数专题；
- 目录标签明确写“理论支撑”或“按需阅读”。

不要在主线和专题区各放一个 04。04 只有一个正式入口，位于专题支撑区。

### 2. `robotics-notes/00-progress.html`

- Ch 02 的主线列表只显示 01、02、03；
- 后续章节整体向前挪一位；
- 04 的线代页面从主线卡片中移除；
- 在“基础支撑”或“专题推荐”区域单独放置线性代数专题；
- 给专题添加说明：

  > 这不是主线下一章，而是用来解释笔记 02 中齐次坐标为什么成立的线代补充。建议学完 02 后按需阅读。

### 3. 页面内部导航

同步修改所有页面顶部导航中的文件名和显示编号。特别检查：

- 01、02、03 顶部导航；
- 原 05～32 页面之间的“上一页 / 下一页”；
- 各阶段自测回跳主线的链接；
- 04 专题页回跳 02、03 的链接。

### 4. `quiz-data.js` 与题库引用

如果题库通过文件名、章节号或页面编号引用主线内容，统一改成新编号。

题目内容里的概念引用也要区分：

- 主线编号引用使用新的编号；
- 线性代数解释引用使用专题标题或新文件名，不写“笔记 04”。

### 5. 其他文档和恢复进度文件

检查以下文件中的旧编号和旧路径：

- `resume.md`
- 其他 `.md` 文件
- HTML 中的 `href`
- JavaScript 中的页面路径字符串
- 任何学习进度、导航、面包屑和返回链接

## 五、04 专题页面的内容整改

04 被移到专题区后，页面内容仍然保留两个试验台，但理论只讲自己独有的线代解释。

### 保留

- 线性变换的 (M\mathbf{0}=\mathbf{0}) 证明；
- 平移为什么不能由同维矩阵完成；
- `linearCanvas` 试验台；
- 补一个 1 的分块矩阵展开；
- `directionCanvas` 试验台；
- 两个点相减得到 (w=0) 的推导；
- “齐次”名称的简短数学背景；
- 二维数值验证。

### 压缩或改成跳转

- 机器人为什么需要齐次变换：回到 02；
- 齐次矩阵四个分区的工程含义：回到 02；
- 多段变换如何连乘：回到 03；
- 点和方向在机器人中的完整用法：回到 02；
- 重复的长篇工程师实战说明：删掉或压缩成一句。

### 专题页开头建议

```text
笔记 02 已经讲过齐次矩阵怎么用，笔记 03 已经讲过变换怎么连乘。
本专题不重新讲机器人用法，只回答一个线代问题：
为什么普通矩阵不能平移，而补上最后一维后就可以？
```

### 专题页跳转

至少保留：

- 回到笔记 02：齐次变换矩阵；
- 回到笔记 03：坐标变换链；
- 回到专题支撑目录。

链接使用稳定的页内锚点，不要依赖“第几节”这种容易随改版变化的文字。

## 六、编号迁移检查表

执行重命名时按下面的对应关系检查：

| 旧编号 | 新编号 | 处理 |
|---:|---:|---|
| 01 | 01 | 保持 |
| 02 | 02 | 保持 |
| 03 | 03 | 保持 |
| 04 | 无 | 移入专题支撑，不占主线编号 |
| 05 | 04 | 文件名、页面标题、目录和链接全部更新 |
| 06 | 05 | 同上 |
| 07 | 06 | 同上 |
| 08 | 07 | 同上 |
| 09 | 08 | 同上 |
| ... | ... | 统一减 1 |
| 32 | 31 | 文件名、页面标题、目录和链接全部更新 |

注意：这里的“旧编号”是当前页面的全局编号，不是 PDF 原书章节号。原书章节号、公式编号和教材页码不要跟着机械重编号。

## 七、验收标准

- 主线导航显示为连续编号，没有 `03 → 05`；
- 主线中不再出现线性代数 04；
- 专题支撑区没有主线数字编号；
- 原 05 页面已经正式显示为 04，原 32 页面已经正式显示为 31；
- 所有文件名、页面标题、导航、返回链接、阶段自测和题库引用一致；
- 04 专题的两个试验台、滑块、播放按钮和公式同步正常；
- 04 不再重复完整讲解 02 的齐次矩阵用法和 03 的变换链；
- 所有公式正常渲染，没有裸 LaTeX；
- 所有回顾链接可点击且目标存在；
- 运行 `git diff --check`，不能新增行尾空格；
- 使用 `rg` 搜索旧文件名，确认没有遗留失效链接；
- 打开学习看板和主导航，人工点击主线前后页及专题跳转，确认路径正确。

## 八、给执行者的任务描述

> 请按本方案重构笔记目录：把线性代数与齐次坐标页面从主线 04 移到无编号的“专题专栏·基础支撑”；将原主线 05～32 的所有页面整体向前重编号为 04～31，并同步更新真实文件名、页面标题、导航、进度看板、阶段自测、题库引用、页面互链和恢复文档。04 专题页面保留两个交互试验台，只压缩已经在 02、03 讲过的理论，并用跳转引用。完成后检查主线编号连续、专题区无主线编号、链接无残留、公式正常渲染、交互正常。
